from setuptools import setup, find_packages

setup(
    name='PurBeurreProject',
    version='0.0.1',
    description='Find module for travis',
    author='',
    url='',
    packages=find_packages(),
)
