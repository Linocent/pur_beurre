language: python
python:
  - "3.10.4"
# command to install dependencies
before_script:
  - pip install -r requirements.txt
# command to run test
services:
  - postgresql
script:
  - python manage.py test