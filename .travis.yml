language: python
python:
  - '3.9'
# command to install dependencies
before_script:
  - pip install -r requirements.txt
branches:
  only:
    - staging
services:
  - postgresql
# command to run test
script:
  - python3 manage.py test