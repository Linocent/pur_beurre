language: python
python:
  - "-1"
# command to install dependencies
before_script:
  - pip install -r requirements.txt
branches:
  only:
    - staging
# command to run test
script:
  - python manage.py test