language: python
python:
  - "3.1"
# command to install dependencies
before_script:
  - pip install -r requirements.txt
# command to run test
services:
  - postgresql
branches:
  only:
    - staging
script:
  - python manage.py test